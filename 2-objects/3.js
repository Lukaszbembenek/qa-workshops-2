// Using destructuirng get color out of beer object and console.log it, then console log rest of it's keys and values
const beer = {
    capacity: 500,
    color: 'amber',
    hue: 'light',
    alcohol: 4.2
}

/*CODE HERE*/

console.log(`Beer color is ${color}`)
console.log('Beer details:')
console.log(rest)
